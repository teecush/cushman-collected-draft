# Cushman Collected Static Scaffold

This is a dependency-free static scaffold for the new Cushman Collected site.
It reads public content from `site_export` and does not import old Squarespace
reviews.

Run it from the repository root:

```sh
python3 -m http.server 5174
```

Then open `http://localhost:5174/website/`.
